

const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
const copiaGatitos = [...gatitos]

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = gatitos.slice()

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = Array.from(gatitos)

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = gatitos.map (x=>x)

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = [].concat(gatitos)

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = JSON.parse(JSON.stringify(gatitos))

// const gatitos = ['😺', '🐈', '🐈‍⬛', '🐾']
// const copiaGatitos = []
// for (let i = 0; i < gatitos.length; i++) {
//     copiaGatitos[i] = gatitos[i];
// }

